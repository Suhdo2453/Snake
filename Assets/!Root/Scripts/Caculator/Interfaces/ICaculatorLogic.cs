
public interface ICaculatorLogic
{
    float Addition(params float[] numbers);
    float Subtraction(params float[] numbers);
    float Multiplication(params float[] numbers);
    float Division(params float[] numbers);
}
